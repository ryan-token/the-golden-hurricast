org: thegoldenhurricast
app: questions
service: questions

provider:
  name: aws
  runtime: nodejs24.x
  stage: dev
  region: us-east-2
  profile: tgh-ryan
  environment:
    STAGE: ${opt:stage, self:provider.stage}
    QUESTIONS_TABLE: ${param:questionsTable}

  httpApi:
    shouldStartNameWithService: true
    cors:
      allowedOrigins: ${param:corsOrigins} # stage-specific; see params below
      allowedHeaders:
        - Content-Type
        - Authorization
      allowedMethods:
        - POST
      allowCredentials: true
      exposedResponseHeaders:
        - Special-Response-Header
      maxAge: 6000 # seconds
  
  iam:
    role:
      statements:
        - Effect: 'Allow'
          Action:
            - dynamodb:PutItem
            - dynamodb:Query
            - dynamodb:Scan
          Resource:
            - Fn::GetAtt: [ QuestionsTable, Arn ]

params:
  default:
    questionsTable: Questions-${opt:stage, self:provider.stage}
    slackWebhookUrl: ${ssm:slackWebhookUrl-${opt:stage, self:provider.stage}}
    # Prod-safe origins. Used by every stage unless overridden below.
    corsOrigins:
      - https://thegoldenhurricast.com
      - https://www.thegoldenhurricast.com
  prod:
    shouldMinifyCode: true
  dev:
    shouldMinifyCode: false
    # Local Gatsby dev only talks to the dev backend.
    corsOrigins:
      - http://localhost:8000 # gatsby develop
      - http://localhost:9000 # gatsby serve

package:
  individually: true
  excludeDevDependencies: true
  patterns:
    - '../../shared/**'
  
plugins:
  - serverless-plugin-common-excludes
  
functions:
  submitQuestion:
    handler: api.submitQuestion
    events:
      - httpApi:
          path: /question
          method: POST
  
  forwardQuestionToSlack:
    handler: stream.forwardQuestionToSlack
    events:
      - stream:
          type: dynamodb
          arn:
            Fn::GetAtt: [QuestionsTable, StreamArn]
    environment:
      WEBHOOK_URL: ${param:slackWebhookUrl}

resources:
  Resources:
    QuestionsTable:
      Type: AWS::DynamoDB::Table
      DeletionPolicy: Retain
      Properties:
        TableName: ${param:questionsTable}
        AttributeDefinitions:
          - AttributeName: questionId
            AttributeType: S
        KeySchema:
          - AttributeName: questionId
            KeyType: HASH
        StreamSpecification:
          StreamViewType: NEW_IMAGE
        PointInTimeRecoverySpecification:
          PointInTimeRecoveryEnabled: true
        BillingMode: PAY_PER_REQUEST
