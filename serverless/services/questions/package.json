{
  "name": "questions",
  "version": "1.0.0",
  "description": "Question submission API: stores questions in DynamoDB and forwards them to Slack via a stream handler.",
  "main": "api.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/util-dynamodb": "^3.996.2",
    "ksuid": "^3.0.0"
  }
}
